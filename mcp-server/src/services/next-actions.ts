/**
 * Engage: compute the To-Do list an MLO user would see (spec section 3).
 * Placeholder wired at the composition root so the required-services
 * ToolContext exists now; the query surface (nextActions/today/starred) and
 * the AvailabilityEngine behind it arrive with ticket 08.
 */
export class NextActionsService {}
