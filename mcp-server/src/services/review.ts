/**
 * Reflect: surface everything that erodes trust in the system (spec section
 * 3). Placeholder wired at the composition root so the required-services
 * ToolContext exists now; the query surface (reviewDue/projects/inbox/…)
 * arrives with ticket 08.
 */
export class ReviewService {}
